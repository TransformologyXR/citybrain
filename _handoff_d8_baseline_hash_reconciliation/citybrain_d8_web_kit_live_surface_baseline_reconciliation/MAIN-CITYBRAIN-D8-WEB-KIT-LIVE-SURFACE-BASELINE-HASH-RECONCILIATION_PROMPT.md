# MAIN-CITYBRAIN-D8-WEB-KIT-LIVE-SURFACE-BASELINE-HASH-RECONCILIATION

Use this prompt if running the task directly instead of via `ENTRY_PROMPT.md`.

Perform the reconciliation described in `ENTRY_PROMPT.md`. Keep it read-only against certified upstreams. This is not a feature sprint and not a UI rewrite. It is a baseline hygiene gate before external capture/viewer validation.
