# CityBrain D8 Web+Kit Live Surface Baseline Reconciliation

Run this before external capture/viewer validation if the Web+Kit live-surface validation package contains source/hash baseline mismatches.

Purpose: reconcile maintained-source baseline hashes without changing certified upstream truth, and classify volatile bridge runtime files separately from immutable source/runtime-bundle artifacts.
