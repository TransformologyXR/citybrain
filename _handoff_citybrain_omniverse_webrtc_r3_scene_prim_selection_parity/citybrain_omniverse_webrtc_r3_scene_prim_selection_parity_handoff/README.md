# CityBrain Omniverse WebRTC R3 — Scene Prim Selection Parity Handoff

This handoff is for the next Omniverse/WebRTC product milestone.

## Recommended next task

`MAIN-CITYBRAIN-OMNIVERSE-WEBRTC-R3-SCENE-PRIM-SELECTION-PARITY`

## Purpose

The browser stream and WebUI navigation are now working. The next real product step is not more stream proof. It is proving that **actual Omniverse scene objects are interactable through the CityBrain packet model**.

## Product result

After this task:

- Kit scene object selection updates WebUI.
- WebUI selection/focus updates Kit scene object selection/focus.
- Both use the same CityBrain packet truth.
- Stream remains visual context.
- Evidence/limitations/no-action state remain visible and bounded.

## Follow-on task

`MAIN-CITYBRAIN-OMNIVERSE-WEBRTC-R4-EVENT-OVERLAY-PARITY`

After scene prim selection parity is proven, add event overlays flowing into both Kit and WebUI.
