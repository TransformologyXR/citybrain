# CityBrain D8 Brain Surface Story Queue R1

Run `ENTRY_PROMPT.md`.

This pack should build the first real "brain surface" over validated scenario layers:
1. a situation queue with London + NYC as distinct primary story shapes,
2. drilldowns for each story,
3. woven trust moments (refusal, human stop, uncertainty),
4. capability cutaways (Chicago precedent and Helsinki visual pick only where evidence supports them),
5. DOM/human-smoke checks that reject record-gallery fallback.

Expected outcome:
- PASS if a navigable two-story board renders and remains boundary-clean.
- PARTIAL if only one story can render coherently or the UI collapses into inventory/cards.
