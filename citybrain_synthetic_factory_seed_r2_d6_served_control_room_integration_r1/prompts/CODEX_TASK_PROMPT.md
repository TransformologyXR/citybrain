# Codex Task Prompt — MAIN-CITYBRAIN-SYNTHETIC-FACTORY-SEED-R2-D6-SERVED-CONTROL-ROOM-INTEGRATION-R1

Run the included runner against the locked D5 served-runtime integration output. Create the output root, add a regression test, and append progress.md.

Do not mutate the input roots.

Do not package raw provider payloads or credentials.

Lock as:

`PASS_SYNTHETIC_FACTORY_SEED_R2_D6_SERVED_CONTROL_ROOM_INTEGRATION_R1_WITH_LIMITATIONS`

Report:
- card count
- served response count
- route validation status
- secret scan status
- boundaries preserved
