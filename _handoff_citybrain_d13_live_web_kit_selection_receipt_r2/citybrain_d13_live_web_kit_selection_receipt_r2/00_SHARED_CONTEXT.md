# Shared context — D13 Live Web ↔ Kit Selection Receipt R2

This package follows:
- `PARTIAL_D13_WEB_KIT_SELECTION_SEAM_ONE_WAY_OR_LOG_ONLY`
- D13 rule refresh verifying no second selection model, overlay hashes, and forbidden-command rejection.
- D11 operator gate remains pending real non-builder sessions.
- D14 remains blocked until real `operator_question_corpus.jsonl` exists.

## Durable boundary

Local/LAN/replay/review/query context only. No production/public API claim. No autonomous monitoring, alerting, dispatch, routing/control, enforcement, official ticket/case, approval, legal/certified finding, certified physical geometry, or automated action. Selection handoff is review context only.

## Core rule

The web/operator cockpit owns the selection contract. D13 may add a live transport for that selection:
- web → Kit
- Kit → web

D13 must not create a second selection model. If the selection contract itself must change, stop and produce a stop-and-integrate decision.
