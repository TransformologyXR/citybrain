# CityBrain Metropolis / VSS Narration Runtime Integration R4 Handoff

Task:

```text
MAIN-CITYBRAIN-METROPOLIS-VSS-NARRATION-RUNTIME-INTEGRATION-R4
```

Purpose: promote the R3 guarded partial into an execution-capable VSS narration integration gate.

R3 proved the guardrails and source-class boundary, but VSS did not execute because no VSS command or endpoint was configured. R4 should wire a bounded runtime adapter, execute VSS only when a real command/endpoint is supplied, capture model-generated narration sidecars, and prove that VSS remains narration only.

This is still a bounded local/dev media-evidence lane. It is not production monitoring and it must not create legal findings, official records, alerts-as-commands, routing/control/enforcement, identity inference, biometric inference, or automated action.
