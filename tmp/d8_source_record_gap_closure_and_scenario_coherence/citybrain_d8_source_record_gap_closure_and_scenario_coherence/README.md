# CityBrain D8 Source Record Gap Closure & Scenario Coherence

This pack follows the source-record UI freeze. It does not create new product architecture. It tries to close the remaining source-record blockers and checks whether the current source-backed cards form an honest story.

Run from `ENTRY_PROMPT.md`.

Key principle: if the UI lacks actual city/source records for a claim, show a blocker. Do not replace missing data with smoother prose.
