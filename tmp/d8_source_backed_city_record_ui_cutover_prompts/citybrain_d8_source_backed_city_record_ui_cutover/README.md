# CityBrain D8 Source-Backed City Record UI Cutover

This pack fixes the exact issue the user saw: the UI uses labels from CityBrain architecture and fixtures, not actual city records. It forces a source-record audit and prevents green status if the visible UI still shows fake/generic `Hero...` labels as if they were real city facts.

Run after the Actual-Record-Grounded UI milestone if the page still lacks actual city data.
