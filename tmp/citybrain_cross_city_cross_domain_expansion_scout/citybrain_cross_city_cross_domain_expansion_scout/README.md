# CityBrain Cross-City / Cross-Domain Expansion Scout

Run as the first scout for the next expansion decision. This package is scout-only: it should inventory opportunities, not ingest new data or mutate existing outputs.

Start with `ENTRY_PROMPT.md`.
