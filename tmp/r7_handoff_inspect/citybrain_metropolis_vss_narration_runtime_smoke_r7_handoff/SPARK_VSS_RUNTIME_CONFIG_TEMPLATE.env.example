# Spark VSS R7 runtime config template
# Do not commit real secrets.

CITYBRAIN_SPARK_VSS_READY_ENDPOINT=http://spark-2445:38111/v1/ready

# Option A: endpoint mode
# Confirm route on installed VSS profile.
CITYBRAIN_SPARK_VSS_SUMMARIZE_ENDPOINT=http://spark-2445:38111/v1/summarize

# Option B: command wrapper mode
# CITYBRAIN_SPARK_VSS_COMMAND=/home/txr/works/citybrain-vss-runtime/citybrain_vss_summarize_wrapper.sh

# Inputs
CITYBRAIN_R2_MEDIA_PATH=/path/to/sample_1080p_h264.mp4
CITYBRAIN_R2_PACKAGE=/path/to/METROPOLIS_VSS_OBJECT_METADATA_EXPORT_R2_PACKAGE.zip
CITYBRAIN_R6C_PACKAGE=/path/to/METROPOLIS_VSS_RUNTIME_PROVISIONING_AND_CONFIGURATION_R6C_PACKAGE.zip
