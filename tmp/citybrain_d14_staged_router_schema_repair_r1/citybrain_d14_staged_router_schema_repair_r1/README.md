# D14 Staged Router Schema Repair R1

This pack supersedes further flat route-taxonomy repairs after v0.4B.

The goal is to reframe D14 routing as staged decisions rather than a single flat `expected_route_label`.

Run from `ENTRY_PROMPT.md`.

Expected stop status:

`PAUSED_D14_STAGED_ROUTER_SCHEMA_AWAITING_INDEPENDENT_DOUBLE_LABELS`

Do not open Split/Seal R3 or router preflight until independent double-label gates pass under staged metrics.
