# CityBrain D8 NYC Cascade Scenario Layer Authoring R1

This pack follows the D8 Scenario Authoring R1 result. It tries to convert the NYC MVC/affected-asset cascade from `authoring_plan_only` into a concrete second primary story shape.

Run from `ENTRY_PROMPT.md`.

Expected honest outcomes:
- PASS with limitations if a concrete source-backed NYC cascade scenario layer is authored.
- PARTIAL if existing output records are too thin.

This pack does not redesign UI, capture media, or land new data.
